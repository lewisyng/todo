import React from 'react'
import List from './SingleList.sass'

function SingleList() {
    return (
        <div>
            {/* {items.map((item, i) => {
        return (
            <>
            <DocumentItem
            key={i}
            item={item}
            updateItem={async () =>
              await getItems(currentCollection).then((data) =>
                store.setItems(data)
              )
            }
          />
          </>
        );
      })} */}
        </div>
    )
}

export default List
